# reference/ (local only — git-ignored)

This folder holds the GeneXus **Work With** pattern's .NET sources used as the transcription
reference: copies of
`…/GeneXus18U12PlatformSDK/Patterns/WorkWith/Source` and `…/Definitions`.

These files are **git-ignored** and are **not** part of the published extension — only the
original TypeScript under `src/` ships. See `NOTICE.md` and
`ai/CSHARP-TO-TYPESCRIPT-WORKFLOW.md`.
